<Window x:Class="UmBuster.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UmBuster.ViewModels"
        mc:Ignorable="d"
        Title="UmBuster" Height="70" Width="260"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="True" ResizeMode="NoResize"
        MouseDown="Window_MouseDown">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="IconButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#F0202020" CornerRadius="35" BorderBrush="#444" BorderThickness="1">
        <Grid Margin="20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/> <!-- Counter -->
                <ColumnDefinition Width="*"/>    <!-- Last Word -->
                <ColumnDefinition Width="Auto"/> <!-- Settings -->
                <ColumnDefinition Width="Auto"/> <!-- Play/Pause -->
            </Grid.ColumnDefinitions>

            <!-- Counter -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                <Ellipse Width="10" Height="10" Fill="#FF5555" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding FillerCount}" 
                           Foreground="White" 
                           FontSize="20" 
                           FontFamily="Segoe UI"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- Last Word -->
            <TextBlock Grid.Column="1" 
                       Text="{Binding LastFillerWord}" 
                       Foreground="#AAAAAA" 
                       FontSize="16"
                       FontFamily="Segoe UI"
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center"
                       TextTrimming="CharacterEllipsis"/>

            <!-- Settings Button -->
            <Button Grid.Column="2" Command="{Binding OpenSettingsCommand}" Style="{StaticResource IconButtonStyle}" Margin="0,0,15,0">
                <TextBlock Text="⚙" FontSize="18" FontFamily="Segoe UI Symbol"/>
            </Button>
            
            <!-- Toggle Button -->
            <Button Grid.Column="3" Command="{Binding ToggleWrapperCommand}" Style="{StaticResource IconButtonStyle}">
                <TextBlock FontSize="18" FontFamily="Segoe UI Symbol">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="▶"/> <!-- Play -->
                            <Setter Property="Foreground" Value="#CCCCCC"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                    <Setter Property="Text" Value="⏸"/> <!-- Pause -->
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Button>
            
        </Grid>
    </Border>
</Window>
